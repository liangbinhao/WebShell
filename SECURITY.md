# 安全说明

本项目为**个人使用的 Web SSH 运维工作台**（单用户、本地/内网部署），安全设计面向这一场景。以下是安全模型与已知注意事项。

## 安全设计

### 凭据存储

- **密码**：使用 `cryptography.Fernet` 加密后落盘于 `backend/data/servers.json`（`password_enc` 字段），密钥存于同目录 `secret.key`（权限 0600），解密仅在后端内存中进行。
- **私钥**：存储私钥**路径**（`key_path`），私钥文件本身由用户持有；私钥口令（passphrase）同样加密存储。
- **永不外泄**：密码、口令、私钥路径**绝不**出现在任何 REST 响应中；前端仅在创建/更新时短暂提交。

### 网络边界

- 后端监听 `127.0.0.1`（默认），CORS 仅放行 `localhost`/`127.0.0.1` 来源。
- 前端 vite dev server 仅 `127.0.0.1:5173`，`/api` 与 `/ws` 代理到后端。

### SSH 安全

- **Host key 强校验**：连接使用 `known_hosts` 校验，**绝不默认关闭**。首次连接新服务器会失败，需先执行：

  ```bash
  ssh-keyscan -p <port> <host> >> ~/.ssh/known_hosts
  ```

- SSH keepalive（30s × 3 次）检测断线；连接超时 15s。
- 跳板机（ProxyJump）链上每个跳板独立认证。

### 资源释放

- 每个 WebSocket 连接对应独立 SSH 会话；断开（close 帧、网络异常、服务停止）即释放会话与连接。
- 删除服务器时自动关闭其上的活动会话。

## 已知注意事项

- **数据目录含明文可解密的密文**：`backend/data/` 包含加密后的密码与密钥文件。请确保该目录权限收紧（仅当前用户），且**不要提交到 Git**（已加入 `.gitignore`）。
- **命令历史**包含用户执行过的命令文本（可能含敏感参数），存储于 `backend/data/history.json`（明文）。历史**同命令去重、仅保留最近 200 条**，可按需清理（删除单条或清理整个 history.json）。
- **单用户信任模型**：本项目不设多用户/认证/审计。请勿暴露到公网或不可信网络。如需多用户、RBAC、审计，请参考需求文档 §25 扩展方向。
- **日志**：不记录密码、私钥内容。但连接错误信息可能包含主机名等，请勿将日志对外公开。

## 报告安全问题

本项目当前为个人项目。如发现安全问题，请通过 GitHub Issues 报告（不要公开漏洞利用细节），或直接联系维护者。
