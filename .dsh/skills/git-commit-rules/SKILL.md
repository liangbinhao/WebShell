---
name: git-commit-rules
description: 项目的 Git 提交/推送规则与提交后修正（amend / rebase）。任何 git 提交、推送、commit --amend、rebase，或涉及提交粒度/时机/信息格式的操作前必读。核心：攒够一个逻辑单元 → 展示给用户确认 → 用户同意后才执行；推送与改写历史前也须用户确认。
whenToUse: 每次准备执行 git add / commit / push / commit --amend / rebase 之前，以及用户问"怎么提交/要不要提交/提交了什么/怎么整理提交历史"时。
---

# Git 提交规则

## 核心流程（最重要）

**所有提交（不只推送）前必须经用户确认。** 流程：

1. 改动在工作区**攒够一个可描述的逻辑单元**（功能完整实现 + 测试通过 + 文档同步，或一个完整修复）；
2. 提交前向用户**展示**：改了哪些文件（`git diff --stat`）+ 拟用的提交信息；
3. **用户明确同意后**才 `git commit`；
4. 推送 GitHub 前同样须用户确认。

> 为什么：AI 无法保证一次改动就正确，功能要迭代、bug 要复现验证、文档要和用户对齐。
> 未经确认直接提交 = 把 git 当保存按钮，会产生多次返工提交。攒够 + 确认 = 干净历史 + 用户把关。

## 提交粒度

**一个提交 = 一个可独立描述的逻辑单元（功能 / 修复 / 文档任务），不是一次文件修改。**

- 开发一个功能：**整个功能做完**（实现 + 测试 + 文档）再提交一次；
- 小修复：攒够相关改动合并提交，或并入所属功能；
- 文档更新：随功能一起提交（如 `feat: xxx（含文档）`），或同类文档合并为一次 `docs:`；
- 探索性工作：不产生价值的直接删除不提交；有价值的结论压缩成一份备忘合并提交。

## 提交时机

- ✅ 完成一个可描述的逻辑单元 + 用户确认时；
- ✅ 需要切换任务前（checkpoint，先向用户确认再提交）；
- ❌ 每次文件保存 / 每次命令执行后 / 未经用户确认。

进行中的工作可留在工作区不提交；跨天或切换任务前，先向用户确认是否提交当前进度。

## 提交信息格式

```
<type>(<scope>): <一句话描述>

type: feat / fix / docs / refactor / chore / test
scope: web / backend / script / docs（可选）
```

- 描述用中文，简洁说明"做了什么"；
- 同一主题的多条改动（实现 + 文档）合并在一个提交里，不要拆散；
- 提交信息里避免特殊字符导致 shell 解析问题（用引号包裹整条 message）。

## 提交后修正（amend / rebase）

**`git commit --amend`（把当前小改动追加到上一次提交）**——适用条件**全部满足**才用：
- 上一次提交刚完成、属于**同一个逻辑单元**，发现的是小瑕疵（错别字、漏改一行、补测试断言）；
- 上一次提交**尚未推送**（已推送 → 禁止 amend，改用新提交，不改写已公开的历史）；
- 执行前同样展示 diff 并向用户**确认后才执行**。
- 不适用：把不相关的改动塞进上次提交；为了"少一次提交"而混入无关内容。

**`git rebase`（重写提交历史）**——适用场景：
- 整理**本地未推送**的提交：把多次小提交合并（squash/fixup）成一个逻辑单元，或基于更新的主分支变基以保持线性历史；
- rebase 会**重写提交历史**，任何 rebase 都必须先向用户说明计划（哪些提交会被改写、改成什么样），**获得确认后才执行**；
- 已推送的提交一律不 rebase。
- 默认以"攒够再提交"为主，rebase 主要用于推送前的历史整理，不是日常操作。

## 安全与卫生

- 提交前检查 diff（`git diff --stat`、敏感字段扫描）。
- 不提交敏感信息（密码/私钥/密钥文件）、生成物（.venv / node_modules / dist / data / 缓存）。
- 推送前确认 `.gitignore` 覆盖所有生成物。
