# AGENTS.md

## Project Instructions

本文件定义当前项目中 AI Agent 的**通用骨架规则**。详细细则按需加载（见各章节指针），避免主文件膨胀。

### 0. 项目概览

**Web SSH Workspace**（个人 Web SSH 运维工作台）。需求以 `docs/project/requirements.md` 为准；前后端接口契约以 `docs/project/CONTRACT.md` 为准。

技术栈：

* 前端：React（Vite + TypeScript）+ xterm.js + shadcn/ui（Tailwind CSS）
* 后端：Python 3.11 + FastAPI + WebSocket + AsyncSSH（uv 管理环境）
* 前后端分离：后端仅提供 REST API 与 WebSocket，不渲染页面

目录与结构详见 `docs/project/requirements.md`。

开发约束：

* 终端交互必须基于 **PTY / interactive shell**，禁止退化为 `ssh exec command` 一次性执行；
* 服务器配置、命令库、历史记录三块数据彼此解耦；
* 新代码遵循 `docs/project/requirements.md` 的模块划分，不得为局部方便破坏结构约定。

### 1. General Rules

* 开始工作前，先阅读项目现有文档、代码结构和配置。
* 不要在没有理解现有实现的情况下直接修改代码。
* 优先复用已有实现，避免不必要的重构。
* 保持修改范围与当前任务一致。
* 不要为了完成局部任务而破坏已有功能。
* 避免引入没有必要的复杂度和第三方依赖。
* 如果需求存在歧义、冲突或缺少关键信息，应先说明问题，而不是自行猜测。

### 2. Requirements and Documentation

* 项目需求以项目中的需求文档为准。
* 架构、设计和接口以对应的项目文档为准。
* 修改实现导致文档失效时，应同步更新相关文档。
* 如果代码与现有规范或文档冲突，应先确认冲突原因，再决定是否修改。

### 3. Code Changes

修改代码前：定位模块 → 读上下文 → 理解设计 → 明确范围 → 实现。

修改代码后：检查改动 → 运行相关测试 → 检查副作用 → 确认无无关改动。

不要为了让测试通过而简单绕过、删除或弱化测试。

### 4. Testing

* 新增功能应尽可能提供相应测试；修改已有功能应运行受影响测试。
* 测试失败应分析根因，不得盲目改测试。
* 无法自动验证的内容（如纯视觉、需真实服务）必须明确说明。
* 不得声称执行了未执行的测试或验证。
* **完整测试策略（分层 / bug 先红后绿 / 前端分层 / E2E / AI 协作）：详见 `docs/agent/testing-policy.md`，涉及测试必读。**

### 5. Dependencies

* 新增依赖前确认确实需要、检查已有依赖可否满足、确认兼容当前环境。
* 不为简单功能引入大型框架或不必要依赖。

### 6. Security

* 不硬编码/提交/记录密码、Token、私钥等敏感信息。
* 不为方便开发降低安全检查；发现安全风险主动报告。

### 7. Git

* 提交前检查工作区与 diff；不提交敏感信息、生成物、无关文件。
* **推送 GitHub 前必须经用户确认**；默认只提交本地。
* **完整提交规则（粒度 / 时机 / 信息格式）：详见 `docs/agent/git-policy.md`，提交必读。**

### 8. Agent Collaboration

如果当前任务使用 Agent Team：

* Agent 严格按职责工作，不擅自接管他人职责或修改其核心模块。
* 跨模块问题及时反馈协调者；共享接口/契约变化先协调。
* 明确报告完成情况、修改内容、测试结果与未解决问题。

### 9. Communication

输出风格：**行动先行、步骤编号、无废话**（受 i-have-adhd 启发，见 `docs/novice/` 指南）。

#### 9.1 AI 输出规则

1. **行动先行**：第一行写"做什么"（命令/路径/改动点），不是背景或计划。
2. **多步任务编号**：每步一个动作，不嵌套多个"然后"。
3. **结尾给一个具体下一步**：若有未完成事项，明说"下一步：xxx"。
4. **每轮复述状态**：多步工作中写明"第 N/M 步完成：xxx。下一步：yyy"。
5. **说明改动范围**：动手前声明"将改 A、B；不动 C"。
6. **每条指令给一句"为什么"**：用户是新手时尤其必要——不只给做法，给心智模型。
7. **错误平实**：直接说"哪行错、为什么、怎么修"，不用"Uh oh"式语气。
8. **暴露不确定性**：没把握时明说"依据是 X，建议先验证 Y"，不把猜测当事实。
9. **列表 ≤ 5 项**：超则分"现在做/稍后做"。
10. **无废话**：禁"Great question""Hope this helps"等开场/结尾客套。

完成任务后简洁说明：完成了什么；改了哪些文件；执行了哪些测试及结果；未解决问题；需用户确认事项。不要只报告"完成"。

### 10. Priority

规则冲突时优先级：

1. 用户明确要求；
2. 项目需求和设计文档；
3. 当前 `AGENTS.md`；
4. Agent 自身任务说明；
5. Agent 的默认行为。

无法确定正确行为时，停止扩大修改范围并请求确认。

### 11. 协作护栏（防范围失控 / 防带偏）

与新手用户协作时（或用户要求时），额外遵守：

1. **动手前确认范围**：AI 提议做多件事时，先做用户确认的前几件，不擅自扩大。
2. **破坏性操作双确认**：`rm -rf`、强推、删数据/表、改 schema 等，先说明后果再等确认。
3. **卡壳超 3 轮换策略**：连续"还是不行"时停止硬试，重新表述假设或把问题拆小，不重复无效尝试。
4. **不理解的代码不提交**：用户表示不理解某改动时，先解释到用户能复述，再继续。
5. **概念总结**：每完成一个功能/修复，用一句话总结"这次做了什么、为什么这么做"，帮助建立心智模型。

#### 11.1 学习记录（learned.md）

用户要求记录"学过什么"时，**追加到 `docs/novice/learned.md`**（仅本机、不入 git）。每条固定格式：

```markdown
## YYYY-MM-DD 主题
- 是什么：一句话
- 何时用：触发场景
- 坑：踩过的坑 / 注意事项
```

时机：修完 bug、学完功能、踩坑当下，由用户提出或 AI 主动建议时追加。追加前读文件末尾避免重复主题；写入后向用户确认条目内容。

---

## 细则文档索引

| 主题 | 文件 | 何时读 |
|---|---|---|
| Git 提交规则 | `docs/agent/git-policy.md` | 每次提交前 |
| 测试策略（分层/复现/E2E/AI） | `docs/agent/testing-policy.md` | 涉及测试、修 bug、写测试时 |
| 开发流程与文档联动 | `docs/agent/development-workflow.md` | 功能开发时 |
