# AGENTS.md

## Project Instructions

本文件定义当前项目中 AI Agent 的通用工作规范。

### 0. 项目技术栈与结构

本项目为 **Web SSH Workspace**（个人 Web SSH 运维工作台），需求以 `requirements.md` 为准。

技术栈：

* 前端：React（Vite + TypeScript）+ xterm.js + shadcn/ui（Tailwind CSS）
* 后端：Python + FastAPI + WebSocket + AsyncSSH
* 前后端分离：后端仅提供 REST API 与 WebSocket，不渲染页面

后端建议目录结构（`requirements.md` §20.3）：

```text
app/
├── main.py
├── api/
├── websocket/
├── ssh/
│   ├── manager.py
│   ├── session.py
│   └── connection.py
├── commands/
├── servers/
├── models/
├── config/
└── utils/
```

前端建议按功能模块组织组件（三栏布局：Servers / Terminal Tabs / Commands）。

开发约束：

* 终端交互必须基于 **PTY / interactive shell**，禁止退化为 `ssh exec command` 一次性执行；
* 服务器配置、命令库、历史记录三块数据彼此解耦；
* 新代码遵循 `requirements.md` 的模块划分，不得为局部方便破坏结构约定。

### 1. General Rules

* 开始工作前，先阅读项目现有文档、代码结构和配置。
* 不要在没有理解现有实现的情况下直接修改代码。
* 优先复用已有实现，避免不必要的重构。
* 保持修改范围与当前任务一致。
* 不要为了完成局部任务而破坏已有功能。
* 避免引入没有必要的复杂度和第三方依赖。
* 如果需求存在歧义、冲突或缺少关键信息，应先说明问题，而不是自行猜测。

### 2. Requirements and Documentation

* 项目需求以项目中的需求文档为准。
* 架构、设计和接口以对应的项目文档为准。
* 修改实现导致文档失效时，应同步更新相关文档。
* 如果代码与现有规范或文档冲突，应先确认冲突原因，再决定是否修改。

### 3. Code Changes

修改代码前：

1. 定位相关模块；
2. 阅读必要的上下文；
3. 理解现有设计；
4. 明确修改范围；
5. 再进行实现。

修改代码后：

1. 检查修改内容；
2. 运行相关测试；
3. 检查是否产生副作用；
4. 确认没有无关文件被修改。

不要为了让测试通过而简单绕过、删除或弱化测试。

### 4. Testing

* 新增功能应尽可能提供相应测试。
* 修改已有功能时，应运行受影响的测试。
* 测试失败时，应分析根因，而不是盲目修改测试。
* 无法实际验证的内容必须明确说明。
* 不得声称已经执行了实际上没有执行的测试或验证。

### 4.1 开发流程约定

功能开发按以下顺序推进（参照 `requirements.md`）：

1. 定位需求文档对应章节（功能需求 → 验收标准）；
2. 明确接口契约：REST 路径、WebSocket 消息格式、数据模型；
3. 后端先行实现（API / WebSocket / SSH 会话），再实现前端对接；
4. 实现后补充或更新测试；
5. 更新受影响的文档（`requirements.md` 或本文件）。

约定：

* API 变更必须同步前端 API 层，禁止前后端各写一份不一致的契约；
* WebSocket 消息需定义消息类型字段（如 `type`），新增类型时同步文档；
* 前后端并行开发时，先以契约（接口定义）为准，接口变更需提前说明；
* 涉及 SSH 会话、终端数据流的改动，必须验证断线、异常关闭等边界场景；
* 完成一个功能模块后再开始下一个，避免多个半成品模块堆积。

### 5. Dependencies

新增依赖前：

* 确认确实需要；
* 检查项目已有依赖是否可以满足需求；
* 优先使用项目已有依赖；
* 确认新的依赖符合当前开发环境和项目要求。

不要为了一个简单功能引入大型框架或不必要的依赖。

### 6. Security

* 不得硬编码密码、Token、Private Key 等敏感信息。
* 不得将敏感信息提交到 Git。
* 不得在日志、测试输出或 Debug 信息中泄露敏感信息。
* 不得为了方便开发而随意降低安全检查。
* 发现潜在安全问题时，应主动报告。

### 7. Git

* 修改前检查当前工作区状态。
* 不要覆盖或删除与当前任务无关的用户修改。
* 不要执行可能导致已有工作丢失的操作。
* 提交代码前检查 diff。
* 不提交敏感信息、临时文件或无关生成文件。

### 8. Agent Collaboration

如果当前任务使用 Agent Team：

* Agent 应严格按照自己的职责工作。
* 不要擅自接管其他 Agent 的职责。
* 不要未经协调修改其他 Agent 负责的核心模块。
* 跨模块问题应及时反馈给负责协调的 Agent。
* 共享接口、Contract 或设计发生变化时，应先协调相关 Agent。
* Agent 应明确报告自己的完成情况、修改内容、测试结果和未解决问题。

### 9. Communication

完成任务后，应简洁说明：

* 完成了什么；
* 修改了哪些文件；
* 执行了哪些测试；
* 测试结果；
* 是否存在未解决的问题；
* 是否存在需要其他 Agent 或用户确认的事项。

不要只报告“完成”。

### 10. Priority

当多个规则发生冲突时，优先级为：

1. 用户明确要求；
2. 项目需求和设计文档；
3. 当前 `AGENTS.md`；
4. Agent 自身任务说明；
5. Agent 的默认行为。

如果无法确定正确行为，应停止扩大修改范围，并请求确认。
