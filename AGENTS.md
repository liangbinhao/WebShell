# AGENTS.md

## Project Instructions

本文件定义当前项目中 AI Agent 的**通用骨架规则**。详细细则按需加载（见文末索引），避免主文件膨胀。

## 1. 内容准入（控制本文件膨胀）

**任何新规则要进入本文件，先过三问，全部满足才可写入；否则做成 skill（`.dsh/skills/<名>/SKILL.md`，含 YAML frontmatter），本文件只留指针：**

1. **是否每次会话都需要？** 只特定任务用 → skill（按需加载）。
2. **是否一句话能说清？** 需例子/边界/多步说明 → skill（可带 Bad/Good 示例）。
3. **是否约束通用行为？** 只约束某模块/场景 → 对应文档（README/API 等）。

**配套纪律**：
- 修改已存在的细则时，优先更新对应 skill 文件，不回流本文件。
- 新增小节前先评估：能否并入现有小节？能否压缩为一条？
- 本文件目标控制在 ~150 行以内；超限时审查可下沉到细则的内容。

## 2. 项目概览

**Web SSH Workspace**（个人 Web SSH 运维工作台）。需求以 `docs/project/requirements.md` 为准；前后端接口契约以 `docs/project/CONTRACT.md` 为准。

技术栈：

* 前端：React（Vite + TypeScript）+ xterm.js + shadcn/ui（Tailwind CSS）
* 后端：Python 3.11 + FastAPI + WebSocket + AsyncSSH（uv 管理环境）
* 前后端分离：后端仅提供 REST API 与 WebSocket，不渲染页面

目录与结构详见 `docs/project/requirements.md`。

开发约束：

* 终端交互必须基于 **PTY / interactive shell**，禁止退化为 `ssh exec command` 一次性执行；
* 服务器配置、命令库、历史记录三块数据彼此解耦；
* 新代码遵循 `docs/project/requirements.md` 的模块划分，不得为局部方便破坏结构约定。

## 3. General Rules

* 开始工作前，先阅读项目现有文档、代码结构和配置。
* 不要在没有理解现有实现的情况下直接修改代码。
* 优先复用已有实现，避免不必要的重构。
* 保持修改范围与当前任务一致。
* 不要为了完成局部任务而破坏已有功能。
* 避免引入没有必要的复杂度和第三方依赖。
* 如果需求存在歧义、冲突或缺少关键信息，应先说明问题，而不是自行猜测。
* **事实陈述须有依据**：涉及数量/状态/版本/时间等可查询事实（未推送提交数、测试结果、端口、文件状态等），必须先执行对应工具查询再报告，禁止凭记忆给出具体数字；未查询时明确说"需查询确认"，不给看似精确的猜测值。会话记忆只用于过程描述，当前状态一律以最新工具结果为准。事实错误时直接承认纠正，不辩解。

## 4. Requirements and Documentation

* 项目需求以项目中的需求文档为准。
* 架构、设计和接口以对应的项目文档为准。
* 修改实现导致文档失效时，应同步更新相关文档。
* 如果代码与现有规范或文档冲突，应先确认冲突原因，再决定是否修改。

## 5. Code Changes

修改代码前：定位模块 → 读上下文 → 理解设计 → 明确范围 → 实现。

修改代码后：检查改动 → 运行相关测试 → 检查副作用 → 确认无无关改动。

不要为了让测试通过而简单绕过、删除或弱化测试。

## 6. Testing

* 新增功能应尽可能提供相应测试；修改已有功能应运行受影响测试。
* 测试失败应分析根因，不得盲目改测试。
* 无法自动验证的内容（如纯视觉、需真实服务）必须明确说明。
* 不得声称执行了未执行的测试或验证。
* **完整测试策略（分层 / bug 先红后绿 / 前端分层 / E2E / AI 协作）：调用 skill `testing-strategy` 获取，涉及测试必读。**

## 7. Dependencies

* 新增依赖前确认确实需要、检查已有依赖可否满足、确认兼容当前环境。
* 不为简单功能引入大型框架或不必要依赖。

## 8. Security

* 不硬编码/提交/记录密码、Token、私钥等敏感信息。
* 不为方便开发降低安全检查；发现安全风险主动报告。

## 9. Git

* **任何 git 提交/推送/amend/rebase 操作前，先调用 skill `git-commit-rules` 加载完整规则再执行。**
* 核心：改动**攒够一个逻辑单元**后，向用户展示 diff + 拟用提交信息，**用户确认后才 commit**；推送、amend、rebase（改写历史）同样需确认。不把 git 当保存按钮（未经确认不提交）。
* 提交前检查工作区与 diff；不提交敏感信息、生成物、无关文件。
* **完整规则（粒度 / 时机 / 格式 / 确认流程 / amend-rebase）：调用 skill `git-commit-rules` 获取。**

## 10. Agent Collaboration

如果当前任务使用 Agent Team：

* Agent 严格按职责工作，不擅自接管他人职责或修改其核心模块。
* 跨模块问题及时反馈协调者；共享接口/契约变化先协调。
* 明确报告完成情况、修改内容、测试结果与未解决问题。

## 11. Communication

输出风格：**行动先行、步骤编号、无废话**。要点：
- 第一行是"做什么"；多步任务编号；结尾给具体下一步；每轮复述进度（第 N/M 步）。
- 动手前声明改动范围；每条指令给一句"为什么"（新手尤其必要）；错误平实直接给根因。
- 暴露不确定性；列表 ≤ 5 项；禁"Great question / Hope this helps"类客套。
- 汇报：完成了什么 / 改了哪些文件 / 测试结果 / 未解决问题 / 需确认事项。

**完整细则（AI 输出规则 + 协作护栏 + 学习记录）：调用 skill `user-communication` 获取，涉及沟通必读。**

## 12. Priority

规则冲突时优先级：

1. 用户明确要求；
2. 项目需求和设计文档；
3. 当前 `AGENTS.md`；
4. Agent 自身任务说明；
5. Agent 的默认行为。

无法确定正确行为时，停止扩大修改范围并请求确认。

## 13. 协作护栏

护栏默认遵守（不判断用户是否新手）：动手前确认范围；破坏性操作双确认；卡壳超 3 轮换策略；不理解的代码解释到用户能复述再继续；概念总结。
- 用户说"这块我已熟悉"→ 跳过详细解释；说"进入正常模式"→ 放宽解释类规则。
- 学习记录（learned.md）位置与格式见 skill `user-communication`。

---

## 细则索引

细则即 skill，统一存放在 `.dsh/skills/<名>/SKILL.md`，AI 按需加载；不另设文档副本。

| 主题 | Skill | 何时读 |
|---|---|---|
| Git 提交规则 | `git-commit-rules` | 每次提交/推送/改写历史前 |
| 测试策略（分层/复现/E2E/AI） | `testing-strategy` | 涉及测试、修 bug、写测试时 |
| 开发流程与文档联动 | `feature-development` | 功能开发时 |
| 沟通协作（输出/护栏/学习记录） | `user-communication` | 涉及沟通规范时 |
